# 🚀 Быстрый старт: Развертывание через Dokploy

## Шаг 1: Настройка проекта в Dokploy

1. Создайте новый проект (Application) в Dokploy
2. Выберите ваш GitHub репозиторий
3. Настройте следующие параметры:

```
Build Context: /
Dockerfile Path: apps/site/Dockerfile
Port: 3100
Working Directory: /app
```

## Шаг 2: Переменные окружения

Добавьте в настройках проекта:

```env
NODE_ENV=production
PORT=3100
HOSTNAME=0.0.0.0

# Database (PostgreSQL)
DATABASE_URL=postgresql://username:password@postgres-container:5432/database_name

# URLs (замените на ваши реальные домены)
ALTRP_SITE_URL=https://your-domain.com
ALTRP_CMS_URL=https://your-cms-domain.com
ALTRP_APP_URL=https://your-app-domain.com

# Auth (сгенерируйте секретные ключи)
NEXTAUTH_SECRET=your-secret-key-here
AUTH_SECRET=your-auth-secret-key-here
```

**Генерация секретов:**
```bash
openssl rand -base64 32
```

**Важно**: 
- `DATABASE_URL` должен быть в формате: `postgresql://username:password@host:port/database_name`
- Если PostgreSQL в отдельном контейнере Dokploy, используйте имя сервиса как host (например, `postgres-container`)
- Если PostgreSQL на том же VPS, используйте IP адрес или `localhost`

## Шаг 3: Миграции базы данных

✅ **Миграции выполняются автоматически** при старте контейнера!

Скрипт миграций:
- Автоматически конвертирует SQLite синтаксис в PostgreSQL
- Выполняет миграции в правильном порядке
- Отслеживает выполненные миграции через таблицу `_migrations`

Если нужно выполнить миграции вручную:
```bash
npm run migrate:postgres
```

## Шаг 4: Развертывание

1. Нажмите "Deploy" в Dokploy
2. Проверьте логи на ошибки
3. Убедитесь, что миграции выполнены успешно (проверьте логи контейнера)
4. Настройте домен и SSL

## ⚠️ Важно - Ограничения

### Cloudflare Pages Functions

Проект использует Cloudflare Pages Functions (`/functions/api/*`) для API endpoints. Эти функции **не будут работать** в обычном Docker окружении.

**Что нужно сделать:**

1. **Мигрировать API endpoints** из `functions/api/*` в Next.js API routes `src/app/api/*`
2. Адаптировать код для работы с PostgreSQL (частично уже сделано)
3. Обновить пути в frontend коде

### База данных

✅ Проект адаптирован для работы с PostgreSQL через Drizzle ORM. Код автоматически определяет окружение:
- **Cloudflare**: D1 Database (SQLite)
- **Dokploy/Docker**: PostgreSQL (через `DATABASE_URL`)

## 📚 Подробная инструкция

См. [DOKPLOY_SETUP.md](./DOKPLOY_SETUP.md) для детальной информации.

## 🔧 Команды

```bash
# Выполнить миграции вручную
npm run migrate:postgres

# Проверить подключение к базе данных
# (можно выполнить в контейнере Dokploy)
psql $DATABASE_URL -c "SELECT version();"
```
