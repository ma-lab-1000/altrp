# Развертывание через Dokploy

Это руководство поможет вам развернуть проект `apps/site` через Dokploy на вашем VPS.

## 📋 Предварительные требования

1. Dokploy установлен и работает на вашем VPS
2. Репозиторий GitHub подключен к Dokploy
3. Доступ к VPS для настройки переменных окружения

## 🚀 Шаги развертывания

### 1. Настройка проекта в Dokploy

1. Войдите в панель Dokploy
2. Создайте новый проект (Application)
3. Выберите подключенный GitHub репозиторий
4. Укажите следующие настройки:

   - **Build Context**: `/` (корень репозитория)
   - **Dockerfile Path**: `apps/site/Dockerfile`
   - **Port**: `3100`
   - **Working Directory**: `/app`

### 2. Настройка переменных окружения

В настройках проекта Dokploy добавьте следующие переменные окружения:

```env
NODE_ENV=production
PORT=3100
HOSTNAME=0.0.0.0

# URLs (замените на ваши реальные домены)
ALTRP_CMS_URL=https://your-cms-domain.com
ALTRP_SITE_URL=https://your-site-domain.com
ALTRP_APP_URL=https://your-app-domain.com

# Auth (сгенерируйте секретные ключи)
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# OAuth (опционально, если используете)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
```

**Важно**: 
- `NEXTAUTH_SECRET` должен быть случайной строкой (можно сгенерировать через `openssl rand -base64 32`)
- Замените URL на ваши реальные домены

### 3. Важные ограничения

⚠️ **КРИТИЧЕСКИЕ ЗАМЕЧАНИЯ**:

1. **Cloudflare Pages Functions**: Проект использует Cloudflare Pages Functions (`/functions/api/*`) для API endpoints. Эти функции **не будут работать** в обычном Docker окружении, так как они требуют Cloudflare Workers runtime.

2. **Cloudflare D1 Database**: Проект использует Cloudflare D1 Database, которая недоступна в обычном Docker окружении.

#### Решения:

**Вариант A: Миграция API в Next.js API Routes (рекомендуется)**

Вам нужно будет:
- Создать Next.js API routes в `src/app/api/` для замены Cloudflare Functions
- Адаптировать код для работы с обычной БД (PostgreSQL/MySQL/SQLite) вместо D1
- Обновить репозитории для работы с выбранной БД

**Вариант B: Использование SQLite (временное решение)**

Так как D1 основан на SQLite, можно адаптировать код для работы с SQLite:
- Установить SQLite в Docker контейнере
- Адаптировать репозитории для работы с SQLite через `better-sqlite3` или `drizzle-orm/sqlite`
- Мигрировать API endpoints в Next.js API routes

**Вариант C: Гибридное развертывание**

- Развернуть Next.js frontend через Dokploy
- Оставить API endpoints на Cloudflare Pages
- Настроить CORS и проксирование запросов

### 4. Настройка домена и SSL

1. В настройках проекта Dokploy:
   - Добавьте ваш домен
   - Включите SSL (Let's Encrypt)
   - Настройте проксирование на порт `3100`

### 5. Развертывание

1. Нажмите "Deploy" в Dokploy
2. Дождитесь завершения сборки
3. Проверьте логи на наличие ошибок

## 🔧 Устранение проблем

### Ошибка сборки

- Проверьте, что `Build Context` установлен в `/` (корень репозитория)
- Убедитесь, что `Dockerfile Path` указан как `apps/site/Dockerfile`
- Проверьте логи сборки в Dokploy

### Ошибка запуска

- Проверьте переменные окружения
- Убедитесь, что порт `3100` доступен
- Проверьте логи контейнера

### Проблемы с базой данных

- Убедитесь, что база данных настроена и доступна
- Проверьте подключение к БД
- Выполните миграции базы данных

## 📝 Дополнительные настройки

### Health Check

Вы можете добавить health check endpoint в ваше приложение для мониторинга.

### Автоматическое развертывание

Настройте автоматическое развертывание при push в определенную ветку (например, `main` или `production`).

### Резервное копирование

Настройте регулярное резервное копирование базы данных.

## 🔗 Полезные ссылки

- [Dokploy Documentation](https://dokploy.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Docker Best Practices](https://docs.docker.com/develop/dev-best-practices/)

