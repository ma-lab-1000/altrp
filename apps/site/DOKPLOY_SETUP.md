# Развертывание через Dokploy с PostgreSQL

Это руководство поможет вам развернуть проект `apps/site` через Dokploy на вашем VPS с использованием PostgreSQL.

## 📋 Предварительные требования

1. Dokploy установлен и работает на вашем VPS
2. Репозиторий GitHub подключен к Dokploy
3. PostgreSQL база данных развернута в Dokploy (отдельный контейнер)
4. У вас есть логин и пароль для подключения к PostgreSQL

## 🚀 Шаги развертывания

### 1. Настройка проекта в Dokploy

1. Войдите в панель Dokploy
2. Создайте новый проект (Application)
3. Выберите подключенный GitHub репозиторий
4. Укажите следующие настройки:

   - **Build Context**: `/` (корень репозитория)
   - **Dockerfile Path**: `apps/site/Dockerfile`
   - **Port**: `3100`
   - **Working Directory**: `/app`

### 2. Настройка переменных окружения

В настройках проекта Dokploy добавьте следующие переменные окружения:

```env
NODE_ENV=production
PORT=3100
HOSTNAME=0.0.0.0

# Database (PostgreSQL)
DATABASE_URL=postgresql://username:password@postgres-container:5432/database_name

# URLs (замените на ваши реальные домены)
ALTRP_CMS_URL=https://your-cms-domain.com
ALTRP_SITE_URL=https://your-site-domain.com
ALTRP_APP_URL=https://your-app-domain.com

# Auth (сгенерируйте секретные ключи)
NEXTAUTH_SECRET=your-nextauth-secret-key-here
AUTH_SECRET=your-auth-secret-key-here

# OAuth (опционально, если используете)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
```

**Важно**: 
- `DATABASE_URL` должен быть в формате: `postgresql://username:password@host:port/database_name`
  - Если PostgreSQL в отдельном контейнере Dokploy, используйте имя сервиса как host (например, `postgres-container`)
  - Если PostgreSQL на том же VPS, используйте IP адрес или `localhost`
- `NEXTAUTH_SECRET` и `AUTH_SECRET` должны быть случайными строками (можно сгенерировать через `openssl rand -base64 32`)
- Замените URL на ваши реальные домены

### 3. Настройка подключения к PostgreSQL

Если PostgreSQL развернут в отдельном контейнере Dokploy:

1. Убедитесь, что контейнер PostgreSQL доступен по сети
2. В `DATABASE_URL` используйте имя сервиса контейнера как hostname
3. Убедитесь, что порт PostgreSQL (обычно 5432) открыт для подключения

Пример `DATABASE_URL`:
```
postgresql://myuser:mypassword@postgres-service:5432/mydatabase
```

### 4. Миграции базы данных

Миграции будут выполнены **автоматически** при старте контейнера через entrypoint скрипт.

Скрипт миграций:
- Читает миграции из `migrations/site/`
- Автоматически конвертирует SQLite синтаксис в PostgreSQL
- Выполняет миграции в правильном порядке
- Отслеживает выполненные миграции через таблицу `_migrations`

Если нужно выполнить миграции вручную:
```bash
npm run migrate:postgres
```

### 5. Настройка домена и SSL

1. В настройках проекта Dokploy:
   - Добавьте ваш домен
   - Включите SSL (Let's Encrypt)
   - Настройте проксирование на порт `3100`

### 6. Развертывание

1. Нажмите "Deploy" в Dokploy
2. Дождитесь завершения сборки
3. Проверьте логи на наличие ошибок
4. Убедитесь, что миграции выполнены успешно (проверьте логи контейнера)

## 🔧 Устранение проблем

### Ошибка сборки

- Проверьте, что `Build Context` установлен в `/` (корень репозитория)
- Убедитесь, что `Dockerfile Path` указан как `apps/site/Dockerfile`
- Проверьте логи сборки в Dokploy

### Ошибка подключения к базе данных

- Проверьте `DATABASE_URL` - формат должен быть правильным
- Убедитесь, что PostgreSQL контейнер запущен и доступен
- Проверьте логин и пароль
- Убедитесь, что порт PostgreSQL открыт для подключения
- Проверьте сетевые настройки между контейнерами

### Ошибка миграций

- Проверьте логи контейнера при старте
- Убедитесь, что `DATABASE_URL` установлен правильно
- Проверьте, что у пользователя PostgreSQL есть права на создание таблиц
- Выполните миграции вручную через `npm run migrate:postgres`

### Ошибка запуска

- Проверьте переменные окружения
- Убедитесь, что порт `3100` доступен
- Проверьте логи контейнера

## ⚠️ Важные замечания

### Cloudflare Pages Functions

Проект использует Cloudflare Pages Functions (`/functions/api/*`) для API endpoints. Эти функции **не будут работать** в обычном Docker окружении, так как они требуют Cloudflare Workers runtime.

**Решение**: Вам нужно будет мигрировать API endpoints из `functions/api/*` в Next.js API routes в `src/app/api/*`. Это потребует:

1. Создания Next.js API routes для каждого endpoint
2. Адаптации кода для работы с PostgreSQL вместо D1
3. Обновления путей в frontend коде

### База данных

Проект адаптирован для работы с PostgreSQL через Drizzle ORM. Код автоматически определяет окружение и использует:
- **Cloudflare**: D1 Database (SQLite)
- **Dokploy/Docker**: PostgreSQL (через `DATABASE_URL`)

## 📝 Дополнительные настройки

### Health Check

Вы можете добавить health check endpoint в ваше приложение для мониторинга.

### Автоматическое развертывание

Настройте автоматическое развертывание при push в определенную ветку (например, `main` или `production`).

### Резервное копирование

Настройте регулярное резервное копирование базы данных PostgreSQL.

## 🔗 Полезные ссылки

- [Dokploy Documentation](https://dokploy.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Docker Best Practices](https://docs.docker.com/develop/dev-best-practices/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

